syntax = "proto3";
option java_multiple_files = true;
package com.example.grpcclient;

message HelloMessage{
    string message = 1;
    string name = 2;
}

message HelloResponse {
    string message = 1;
}

service MessageService {
    rpc printMessage(HelloMessage) returns (HelloResponse);
}